# 技术架构

> 本文件由 `tech-architect` skill 生成和维护。
> 记录技术选型、架构决策记录 (ADR — Architecture Decision Record)、复用项目清单和目录结构设计。

## 1. 架构总览

### 1.1 产品类型与架构模式

<!-- 例如：Electron 桌面应用、React SPA 单页应用、CLI 命令行工具 -->

### 1.2 架构图

<!-- 用 Mermaid 或文字描述系统架构骨架 -->

### 1.3 技术栈总览

| 层级 | 选型 | 版本 | 选型理由 |
|------|------|------|---------|
| 前端/客户端框架 | | | |
| UI 组件库 | | | |
| 状态管理 | | | |
| 后端/API 层 | | | |
| 数据库/存储 | | | |
| 构建工具 | | | |
| 测试框架 | | | |
| 部署/托管 | | | |

## 2. 架构决策记录 (ADR)

> 每项关键技术决策写一条 ADR。格式：背景 → 决策 → 备选方案 → 影响。

<!--
### ADR-001: <决策标题>

- **日期：** YYYY-MM-DD
- **状态：** 已采纳

#### 背景
<为什么需要做这个决策？>

#### 决策
<选择了什么？>

#### 备选方案
| 方案 | 优点 | 缺点 | 为什么没选 |
|------|------|------|-----------|
| | | | |
| | | | |

#### 影响
- 正面：
- 负面/风险：
- 缓解：
-->

## 3. 复用项目清单

| 项目 | 覆盖功能 | 协议 | 复用方式 | 可省任务数 |
|------|---------|------|---------|-----------|
| | | | | |

## 4. 自研模块清单

| 模块 | 原因 |
|------|------|
| | |

## 5. 项目规范约束

> 由 tech-architect 向用户收集后归档。如果用户无现有规范，由 tech-architect 基于默认模板生成。
> 后续 dev-planner、implementer、code-review 强制执行，禁止偏离。

### 5.1 目录规范

<!-- 用户有现有规范时记录来源和路径，无规范时用架构设计确认的目录结构 -->
<!-- 示例：
- **来源**：团队 wiki（docs/architecture/project-layout.md）
- **规则**：
  - src/application/ — 入口，禁止写业务逻辑
  - src/business/<模块>/ — 按功能模块分子目录，每模块 handler/service/model 三层
  - src/view/<模块>/ — 按页面分子目录，层层递进
  - src/utils/ — 通用工具，禁止依赖 business/
-->

### 5.2 命名规范

| 类型 | 规则 | 示例 |
|------|------|------|
| 文件名 | <!-- 如：小写 + 连字符 `kebab-case.ts` --> | `user-service.ts` |
| 目录名 | <!-- 如：小写 + 连字符 `kebab-case` --> | `user-profile/` |
| 类名 / 组件名 | <!-- 如：大驼峰 PascalCase --> | `UserProfile.tsx` |
| 函数名 | <!-- 如：小驼峰 camelCase --> | `getUserById()` |
| 变量名 | <!-- 如：小驼峰 camelCase --> | `userList` |
| 常量名 | <!-- 如：全大写 + 下划线 UPPER_SNAKE_CASE --> | `MAX_RETRY_COUNT` |
| 类型 / 接口 | <!-- 如：大驼峰 + I 前缀或无前缀 --> | `IUser` 或 `User` |
| 枚举 | <!-- 如：大驼峰 PascalCase --> | `UserRole.Admin` |

### 5.3 注释规范

| 类型 | 规则 |
|------|------|
| 公共 API / 导出函数 | <!-- 如：必须写 JSDoc / TSDoc，说明参数和返回值 --> |
| 复杂逻辑 | <!-- 如：`// WHY` 解释为什么这样做，不解释做了什么 --> |
| 待办标记 | <!-- 如：`// TODO(@owner): 描述 + 日期` --> |
| 废弃标记 | <!-- 如：`/** @deprecated 用 xxx 替代 */` --> |
| 禁止事项 | <!-- 如：禁止注释掉的代码块，禁止无意义注释（`// 获取用户`） --> |

### 5.4 编码规范

<!-- 按语言/框架分别填写，以下为通用条目 -->

| 规则 | 说明 |
|------|------|
| 类型安全 | <!-- 如：禁止 `any`，必须显式类型 --> |
| 错误处理 | <!-- 如：Promise 必须 try/catch，不吞错误 --> |
| 导入顺序 | <!-- 如：第三方 → 内部模块 → 相对路径 → 样式 --> |
| 文件长度 | <!-- 如：单文件不超过 300 行 --> |
| 函数长度 | <!-- 如：单函数不超过 50 行 --> |
| 副作用 | <!-- 如：禁止模块级副作用，入口文件除外 --> |
| 未使用代码 | <!-- 如：禁止未使用的导入和变量（lint 检查） --> |

## 6. 目录结构设计

```
<project-name>/
├── CLAUDE.md                    # AI 入口：项目上下文，自动加载
├── README.md                    # 人类入口：简介、快速启动
│
├── src/                         # 源代码
│   │
│   ├── application/             # 应用入口（启动配置、路由注册）
│   │
│   ├── business/                # 业务逻辑（前后端分离时）
│   │   └── <模块名>/            #   按功能模块组织
│   │       ├── handler.ts      #     控制器 / 服务入口
│   │       ├── service.ts      #     核心业务逻辑
│   │       └── model.ts        #     数据模型 / 类型定义
│   │
│   ├── view/                    # 视图层（前后端分离时）
│   │   └── <模块名>/            #   按功能模块组织，层层递进
│   │       ├── <页面>.tsx       #     页面组件
│   │       └── components/      #     模块专属 UI 组件
│   │
│   └── utils/                   # 通用工具（不归属任何模块的共享代码）
│
├── docs/                        # 项目文档（AI 快速了解项目的第一入口）
│   │
│   ├── README.md                # 文档导航索引
│   │
│   ├── requirements/            # 需求文档（引用 Product-Spec.md，不重复写）
│   │   ├── overview.md          # 产品概述：一句话定位 + MVP 功能列表 + 目标用户
│   │   └── glossary.md         # 业务术语表
│   │
│   ├── design/                  # 设计文档
│   │   ├── functional/          # 功能设计 — 业务层面
│   │   │   └── <模块名>.md       # 每个模块的交互流程 / UE / 状态处理
│   │   └── visual/              # 原型设计 — UI 层面
│   │       └── <模块名>.md       # 配色 / 组件风格 / 动效 / 对标产品的视觉参考
│   │
│   ├── architecture/            # 架构层 — 系统是什么样的
│   │   └── overview.md          # 系统架构总览（含 Mermaid 图）
│   │
│   ├── modules/                 # 模块实现文档
│   │   ├── README.md            # 模块索引：所有模块清单 + 依赖关系 + 一句话职责
│   │   └── <模块名>/
│   │       └── README.md         # 模块实现详情：关键文件 + 对外接口 + 数据模型 + 状态与边界
│   │
│   └── guides/                  # 操作指南
│       ├── development.md       # 开发环境搭建
│       ├── testing.md           # 测试策略与规范
│       ├── build-and-deploy.md  # 构建与部署
│       └── troubleshooting.md   # 常见问题排查
│
├── tests/                       # 测试
└── scripts/                     # 脚本（CI / 部署 / 自动化）
```

## 6. 数据流设计

<!-- 描述核心数据如何流转：用户操作 → 组件 → 状态管理 → API → 数据库 → 返回 -->

## 7. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| | | |

## 8. 技术错误与修复

> 架构决策本身不放在这里（见第 2 章 ADR）。此处记录技术选型过程中的试错：引入后发现不兼容、性能不达预期、弃用回退等。

| 错误 | 影响 | 修复方式 | 日期 |
|------|------|---------|------|
| | | | |

---

> 关联：[Product-Spec.md](./Product-Spec.md) | [Design-Brief.md](./Design-Brief.md) | [Dev-Plan.md](./Dev-Plan.md)
